server:
  port: 8761

spring:
  application:
    name: Eureka-Server

eureka:
  client:
    # server should not register itself as a service
    register-with-eureka: false
    # prevent fetching its own service list ( client list )
    fetch-registry: false